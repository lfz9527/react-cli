import React from "react"

type Props = {
  data?: string
  info?: string
  type?: string
}
const App: React.FC<Props> = ({ data, info, type }: Props) => {
  return (<div></div>)
}

export default App
